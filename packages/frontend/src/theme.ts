// Ant Design v4 doesn't support theme prop on ConfigProvider
// Dark mode is handled via CSS variables and antd CSS overrides
// This configuration is kept for reference or future v5 upgrade

export const lightThemeConfig = {
  primaryColor: "#1890ff",
};

export const darkThemeConfig = {
  primaryColor: "#177ddc",
};
