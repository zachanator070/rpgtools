import React, {CSSProperties} from 'react';
import {DownOutlined} from "@ant-design/icons";


export default function DownArrowIcon({style}: {style?: CSSProperties}) {
    return <DownOutlined style={style}/>;
}